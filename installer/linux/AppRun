#!/bin/sh
# AppImage entry point - launches the GUI (usr/bin/zftpd, the CLI daemon, sits
# right next to it for anyone who wants a terminal instead).
HERE="$(dirname "$(readlink -f "$0")")"
exec "$HERE/usr/bin/ZFTP" "$@"
